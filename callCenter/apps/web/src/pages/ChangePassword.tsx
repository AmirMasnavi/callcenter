import{FormEvent,useState}from'react';import{api}from'../lib/api';
export default function ChangePassword({done,onLogout}:{done:()=>void,onLogout:()=>void}){const[currentPassword,setCurrent]=useState('');const[newPassword,setNext]=useState('');const[confirm,setConfirm]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();if(newPassword!==confirm){setError('تکرار رمز با رمز جدید یکسان نیست');return}setBusy(true);setError('');try{await api('/api/v1/auth/change-password',{method:'POST',body:JSON.stringify({currentPassword,newPassword})});done()}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
 return <main className="login-page"><section className="login-art"><div className="brand-mark">آ</div><h1>امنیت حساب<br/>از همین‌جا شروع می‌شود</h1><span>رمزی انتخاب کنید که فقط خودتان می‌دانید.</span></section><section className="login-panel"><form className="login-card" onSubmit={submit}><small>اولین ورود</small><h2>تغییر رمز عبور</h2><p>برای ادامه باید رمز موقت خود را تغییر دهید. اگر اشتباهی وارد این حساب شده‌اید، می‌توانید خارج شوید.</p><label>رمز موقت<input type="password" autoComplete="current-password" required value={currentPassword} onChange={e=>setCurrent(e.target.value)}/></label><label>رمز جدید<input type="password" autoComplete="new-password" minLength={10} required value={newPassword} onChange={e=>setNext(e.target.value)}/></label><label>تکرار رمز جدید<input type="password" autoComplete="new-password" minLength={10} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="primary wide" disabled={busy}>{busy?'در حال ثبت…':'ثبت رمز و ادامه'}</button><button type="button" className="link-button wide" onClick={onLogout}>خروج و بازگشت به صفحه ورود</button></form></section></main>}
